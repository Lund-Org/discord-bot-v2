{
  "name": "prisma",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "prisma:migrate": "dotenv -e ../../.env npx prisma migrate --"
  },
  "prisma": {
    "schema": "src/schema.prisma",
    "seed": "ts-node src/seed.ts"
  }
}
